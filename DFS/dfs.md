```c
void  dfs(){//参数表示状态
    if(终点状态){
        //添加
        return;
    }
    if(越界不合法){
        return;
    }
    if(特殊状态){
        //剪枝
        return;
    }
    for (扩展方式)
    {
        if（扩展方式达到合法状态）
        {
            修改操作；
            标记；
            dfs();
            //如果有还原标记，就是回溯方法
        }
    }
}
```